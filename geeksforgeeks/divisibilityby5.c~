#include<stdio.h>
#include<string.h>

int main()
{
	int i,t,n;
	char str[1006];
	scanf("%d",&t);
	
	for(i=0;i<t;i++)
	{
		scanf("%s",str);
		
		n=strlen(str);
		
		if((str[n-1]-48)==5 || (str[n-1]-48)==0)
		{
			printf("%d\n",1);
		}
		else
		{
			printf("%d\n",0);
		}
	}
return 0;
}

