#include<stdio.h>

int main()
{
long long int a,b[100000],c,d,i,j,k,count;
scanf("%lld",&a );


for(i=0;i<a;i++)
{
    scanf("%lld",&c );
count=0;
for(j=0;j<c;j++)
{
scanf("%lld",&b[j]);
}
scanf("%lld",&d);
for(k=0;k<c;k++)
{
if(b[k]<=d)
{
count++;
}
}
printf("%lld\n",count);
}
return 0;
}
