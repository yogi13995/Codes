#include<stdio.h>
#include<math.h>

int main()
{
	int i,t,d,r,a,sum,squar2,j;
	float squar;
	char str[1000];
	scanf("%d",&t);
	
	for(i=0;i<t;i++)
	{
		scanf("%s",str);
		//printf("%d",str);
		
		d=1;
		sum=0;
		j=0;
		while(str[j]!='\0')
		{
			//r=a%10;
			//=a/10;
			//a=d;
			sum = sum+str[j];
			//printf("%d\n",str[j]);
			j++;
		}
		
		squar = sqrt(sum);
		squar2 = sqrt(sum);
			    
	    if(squar == (1.0)*squar2)
	    {
			  printf("%d\n",1);
	    }
		else
		{
			  printf("%d\n",0);
		}
		
			
				
			    
	}
return 0;
}

			    
			
			
		
		
