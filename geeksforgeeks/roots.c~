#include<stdio.h>
#include<math.h>

int main()
{
	int i,t,a,b,c;
	double root1,root2,d;
	scanf("%d",&t);
	for(i=0;i<t;i++)
	{
		scanf("%d %d %d",&a,&b,&c);
		
		d=(b*b)-4*a*c;
		if(d<0)
		{
			
		 	root1=((-b+sqrt(-d))/(2*a));
		 	root2=((-b-sqrt(-d))/(2*a));
		 	printf("COMPLEX\n");
		 	
		 }
		 else
		 {
		 	
		 	root2=((-b-sqrt(d))/(2*a));
		 	root1=((-b+sqrt(d))/(2*a));
		 	printf("%.7f  %.7f\n",root1,root2);
		 }
		
	}
return 0;
}
		 				
