#include<stdio.h>
#include<math.h>

int main()
{
int i,t,a,b,n,r;
long long int x,c,d;
scanf("%d",&t);
	for(i=0;i<t;i++)
	{
		scanf("%d %d %d",&a,&b,&n);
		
		x=pow(a,b);
		c=pow(10,n-1);
		d=x/c;
		r=d%10;
		printf("%d\n",r);
	}
return 0;
}

