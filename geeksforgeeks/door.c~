#include<stdio.h>
#include<string.h>

int main()
{
int i,j,k,l,t,n,count,r,a[1000];
scanf("%d",&t);

	for(i=0;i<t;i++)
	{
		memset(a,0,4000);
	scanf("%d",&n);
	
		for(j=1;j<=n;j++)
		{
			count=0;
			for(k=1;k<=j;k++)
			{
				r=j%k;
		
				if(r==0)
				{
					count++;
					a[j]=a[j]^1;
				}
			}
		}
		for(l=1;l<=n;l++)
		{
			printf("%d",a[l]);
			p
		}
	}
return 0;
}
	
