#include<stdio.h>

int main()
{ 
	int i,n,t;
	long long unsigned int j,sum;
	scanf("%d",&t);
	for(i=0;i<t;i++)
	{
		scanf("%d",&n);
		sum=0;
		for(j=1;j<=n;j++)
		{
			sum=sum+(j*j*j*j*j);
			
        }
        printf("%llu\n",sum);
     }
return 0;
}
