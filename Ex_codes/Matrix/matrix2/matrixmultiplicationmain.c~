#include"matrixmultiplication2.h"

int main()
{
	
	
	 inputOfMatrix(A_leftInput.matrixColoumn, A_leftInput.matrixRaw, A_leftInput.matrix);
	 
	 inputOfMatrix(B_rightInput.matrixColoumn, B_rightInput.matrixRaw, B_rightInput.matrix);
	 
	 inputOfMatrix2(C_output.matrixColoumn, C_output.matrixRaw, C_output.matrix);
	 
	
	 showMatrix(A_leftInput.matrixColoumn, A_leftInput.matrixRaw, A_leftInput.matrix);
	
	 showMatrix(B_rightInput.matrixColoumn, B_rightInput.matrixRaw, B_rightInput.matrix);
	
	
	
	 multiplyMatrix(A_leftInput.matrixColoumn, A_leftInput.matrixRaw, B_rightInput.matrixColoumn, B_rightInput.matrixRaw, C_output.matrixColoumn, C_output.matrixRaw, A_leftInput.matrix, B_rightInput.matrix, C_output.matrix);
	
	 showMatrix2(C_output.matrixColoumn, C_output.matrixRaw, C_output.matrix);
